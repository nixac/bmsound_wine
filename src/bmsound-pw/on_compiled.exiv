# Includes
copy    "$(ProjectDir)bmsound_pw.h"                                         "$(TargetDir)include/"

#_REM Junc (for consistency with current make, probably should be refactored later)
junc    "$(TargetDir)include/bmsound-pw"                                    "$(TargetDir)/include"     1
